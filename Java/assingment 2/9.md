Nice Rahul 👍 Let’s move to **Question 9**.
This one models a **Bank Account system**, which is a classic **OOP example**.

---

# Q9 – Bank Account Class

### Data Members

* `name` → Name of depositor
* `accNo` → Account number
* `type` → Account type
* `balance` → Balance amount

---

### Member Functions

1️⃣ Assign initial values
2️⃣ Deposit amount
3️⃣ Withdraw amount (after checking balance)
4️⃣ Display name and balance

Program should handle **N customers**.

---

# 💻 Java Program

```java
import java.util.Scanner;

class BankAccount {

    String name;
    int accNo;
    String type;
    double balance;

    void assign(String n, int a, String t, double b) {
        name = n;
        accNo = a;
        type = t;
        balance = b;
    }

    void deposit(double amount) {
        balance = balance + amount;
    }

    void withdraw(double amount) {

        if(balance >= amount) {
            balance = balance - amount;
        }
        else {
            System.out.println("Insufficient Balance");
        }
    }

    void display() {
        System.out.println("Name: " + name);
        System.out.println("Balance: " + balance);
    }
}

public class BankTest {

    public static void main(String args[]) {

        Scanner sc = new Scanner(System.in);

        System.out.print("Enter number of customers: ");
        int n = sc.nextInt();

        BankAccount acc[] = new BankAccount[n];

        for(int i = 0; i < n; i++) {

            acc[i] = new BankAccount();

            System.out.println("\nEnter details for customer " + (i+1));

            System.out.print("Name: ");
            String name = sc.next();

            System.out.print("Account No: ");
            int accNo = sc.nextInt();

            System.out.print("Account Type: ");
            String type = sc.next();

            System.out.print("Initial Balance: ");
            double bal = sc.nextDouble();

            acc[i].assign(name, accNo, type, bal);
        }

        for(int i = 0; i < n; i++) {

            System.out.println("\nCustomer " + (i+1));

            acc[i].deposit(1000);
            acc[i].withdraw(500);
            acc[i].display();
        }
    }
}
```

---

# Program Flow

### Step 1 – Create Array of Accounts

```
BankAccount acc[]
```

Example

```
acc[0]
acc[1]
acc[2]
```

Each represents a **customer account**.

---

# Step 2 – Assign Initial Values

Example

```
Name = Rahul
Account No = 101
Type = Saving
Balance = 5000
```

Stored inside object.

---

# Step 3 – Deposit

```
deposit(1000)
```

New balance

```
5000 + 1000 = 6000
```

---

# Step 4 – Withdraw

```
withdraw(500)
```

Check:

```
balance >= 500 ✔
```

New balance

```
6000 - 500 = 5500
```

---

# Example Output

```
Customer 1
Name: Rahul
Balance: 5500
```

---

# Real World Example

This is exactly how **banking software works**:

```
Account
 ├─ deposit()
 ├─ withdraw()
 └─ checkBalance()
```

---

# Important OOP Idea

Each customer has **their own object**.

```
Rahul → Account Object
Amit  → Account Object
Neha  → Account Object
```

Objects store **different data but same structure**.

---

# Common Beginner Mistake

❌ Forgetting balance check

Wrong

```java
balance = balance - amount;
```

Correct

```java
if(balance >= amount)
```

---

# 🧠 Active Recall

Try answering:

1️⃣ Why do we create **array of BankAccount objects**?
2️⃣ Why must we check **balance before withdrawal**?
3️⃣ What will happen if withdrawal amount is greater than balance?

---

Next question is **short but important**.

**Q10 – Method Overloading (add numbers & strings)**
This introduces **compile-time polymorphism**.

Just say **next** and we’ll continue.
